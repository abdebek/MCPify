name: Publish to NuGet

# Trusted Publishing (nuget.org):
# - Policy workflow file name must be: publish-nuget.yml
# - Repository: abdebek/MCPify
# - Environment: leave empty (this job does not set environment:)
# - nuget.org username for NuGet/login is the public profile slug (default: abdebek).
#   Override with secrets/vars.NUGET_USER or workflow_dispatch nuget_user if needed.
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to publish (e.g. 0.0.15-preview)'
        required: true
      nuget_user:
        description: 'Optional nuget.org username override (default: abdebek)'
        required: false
        type: string
        default: ''

jobs:
  publish:
    runs-on: ubuntu-latest
    # No `environment:` — matches empty Environment on nuget.org Trusted Publishing policy
    permissions:
      contents: write
      id-token: write   # required for GitHub OIDC → NuGet short-lived API key

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Resolve NuGet username
      id: nuget_user
      env:
        SECRET_USER: ${{ secrets.NUGET_USER }}
        VAR_USER: ${{ vars.NUGET_USER }}
        INPUT_USER: ${{ github.event.inputs.nuget_user }}
      run: |
        # Prefer: workflow_dispatch input → secret → repository variable → package owner default
        # Public profile: https://www.nuget.org/profiles/abdebek (not email, not API key)
        USER_NAME="${INPUT_USER:-}"
        if [ -z "$USER_NAME" ]; then USER_NAME="${SECRET_USER:-}"; fi
        if [ -z "$USER_NAME" ]; then USER_NAME="${VAR_USER:-}"; fi
        if [ -z "$USER_NAME" ]; then USER_NAME="abdebek"; fi
        echo "name=$USER_NAME" >> "$GITHUB_OUTPUT"
        echo "Using nuget.org username: $USER_NAME"

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        # Multi-target: net8.0;net9.0;net10.0
        dotnet-version: |
          8.0.x
          9.0.x
          10.0.x

    - name: Set version from tag
      if: startsWith(github.ref, 'refs/tags/v')
      run: |
        VERSION=${GITHUB_REF#refs/tags/v}
        echo "PACKAGE_VERSION=$VERSION" >> $GITHUB_ENV

    - name: Set version from input
      if: github.event_name == 'workflow_dispatch'
      run: |
        echo "PACKAGE_VERSION=${{ github.event.inputs.version }}" >> $GITHUB_ENV

    - name: Restore dependencies
      run: dotnet restore MCPify.slnx

    - name: Build
      run: dotnet build MCPify.slnx --configuration Release --no-restore -p:Version=${{ env.PACKAGE_VERSION }}

    - name: Test
      run: |
        dotnet test MCPify.slnx --configuration Release --no-build --verbosity normal \
          --filter "Category!=Playwright" \
          --logger "console;verbosity=detailed"

    - name: Pack
      run: |
        dotnet pack MCPify/MCPify.csproj \
          --configuration Release \
          --no-build \
          --output ./artifacts \
          -p:PackageVersion=${{ env.PACKAGE_VERSION }} \
          -p:Version=${{ env.PACKAGE_VERSION }}

    - name: NuGet login (OIDC → temporary API key)
      uses: NuGet/login@v1
      id: login
      with:
        # nuget.org profile username (Trusted Publishing), NOT email and NOT a long-lived API key
        user: ${{ steps.nuget_user.outputs.name }}

    - name: Publish to NuGet (Trusted Publishing)
      run: |
        # Include .nupkg and .snupkg (symbols)
        shopt -s nullglob
        packages=(./artifacts/*.*nupkg)
        if [ ${#packages[@]} -eq 0 ]; then
          echo "No packages found in ./artifacts"
          ls -la ./artifacts || true
          exit 1
        fi
        for pkg in "${packages[@]}"; do
          echo "Pushing $pkg"
          dotnet nuget push "$pkg" \
            --api-key "${{ steps.login.outputs.NUGET_API_KEY }}" \
            --source https://api.nuget.org/v3/index.json \
            --skip-duplicate
        done

    - name: Delete existing release if it exists
      if: startsWith(github.ref, 'refs/tags/v')
      continue-on-error: true
      run: |
        TAG_NAME=${GITHUB_REF#refs/tags/}
        gh release delete "$TAG_NAME" --yes || true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Create GitHub Release
      if: startsWith(github.ref, 'refs/tags/v')
      uses: softprops/action-gh-release@v2
      with:
        files: |
          ./artifacts/*.nupkg
          ./artifacts/*.snupkg
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
